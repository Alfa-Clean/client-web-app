# TODO — Client Web App

## Тестирование на устройстве

- [ ] Настроить ngrok/tunnel для HTTPS (Telegram Mini App требует HTTPS)
- [ ] Протестировать полный флоу на реальном телефоне через Telegram

## OTP — что осталось за пределами фронта

- [ ] **Вход из браузера неполноценен, пока бэкенд не научится отдавать заказы и
      адреса по клиенту из JWT.** Сейчас `GET /cleaning/orders`, `GET /handyman/orders`
      и адреса фильтруются по `?telegram_id=`, а у клиента, вошедшего по номеру,
      его нет — экран открывается, но списки пустые
- [ ] Убрать `request_contact` и `handlers/booking.py` в `telegram-bot` (ТЗ §7)
